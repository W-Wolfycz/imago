# 更新日志

## 1.1.2 — 2026-09-03

### 修复

- **错误详情透出**：图片节点 4xx/5xx 错误改为提取 OpenAI/Gemini 风格嵌套 `error` 对象（message/type/code）与百炼顶层 code/message，非 JSON 响应回退原始文本（脱敏截断）；修复中转站已返回具体报错却被忽略、日志只剩 HTTP 状态码的问题。
- **失败原因透传配文**：后台任务失败/超时后，主动配文请求携带脱敏截断后的失败原因（如 `HTTP 400 message=… code=…`），LLM 可按人设自然转述原因，不再只有固定“绘制失败”文案；固定文案回退路径不变。

### 变更

- **测试精简**：按「关键节点/易错函数」原则把测试从 181 项精简到 85 项——移除重环境入口测试文件与低价值矩阵用例，错误文案白名单与 Provider 错误详情提取下沉为纯函数单测。

测试：85 项单元测试通过。

## 1.1.1 — 2026-08-30

### 修复

- **配文人设数据源**：拍照任务与主动配文的人设解析改用会话命中的 UMO 配置（`get_config(umo)`，与主链 `_decorate_llm_request` 一致），修正 1.1.0 全局配置优先在多配置文件（umop 路由）场景下把 default 配置人设用于任务、与群里实际对话人设不一致的问题。
- **附加参数保留键拦截**：`--n/--size/--model/--prompt/--count/--messages` 等保留键改为创建阶段报错拒绝，修复可通过附加参数绕过数量上限（成本放大）或覆盖节点配置的问题。
- **错误文案白名单**：修复 `_safe_creation_error` 白名单只定义不匹配的问题（“绘图额度不足”“不允许的附加参数”等此前一直显示为“任务参数无效”），summary/上传命令统一走白名单并补 try/except。
- **摘要重建重复执行**：修复 `/imago summary-rebuild` 重建执行两次、重复产生 LLM 成本的问题。
- **预生成配文预算守卫**：任务剩余不足 20 秒时不再等待预生成配文，修复图片已生成却被拖成 TIMED_OUT 且不发送的边界。
- **降级注入语义**：副脑降级注入只写所选风格预设与默认第三方视角，不再把面向副脑的元指令（optimizer_prompt）写进图片 prompt。
- **参考图关系声明计数**：改为按各节点 `reference_image_limit` 实际采样数量追加，修复 prompt 内人设图数量虚高。
- **前台远程图下载超时**：命令处理链内的 HTTP 参考图下载加 30 秒上限（会话层无读超时），避免慢服务器挂死命令。
- **主动发送记账与超时通知**：装饰链清空结果且未 stop 时记投递失败不再误报成功；超时补发终态通知以主发送是否完成为准，避免重复打扰。

### 新增

- **入口级测试**：新增命令/工具入口、人设解析数据源、`_finish_task` 终态链、主动发送、ChatMemory 接管、Provider 请求体/响应解析的行为级测试。

测试：178 项单元测试通过。

## 1.1.0 — 2026-08-20

### 修复

- **参考图使用语义**：显式参考图存在时向最终 prompt 追加低优先级关系声明，区分“前 N 张用户指定参考”与“其余人设固定图”，修复指令触发的改图/仿图任务不遵循参考图的问题。
- **配文人设数据源**：主动配文与拍照任务的人设解析与主链对齐（provider_settings 全局优先、umo 回退），修复多配置文件场景下配文误用其他人设的问题。
- **WebUI 布局**：宽视口（≥1400px）下布局铺满，不再受 1220px 上限限制。
- **失败配文措辞**：无图（失败/超时）时配文 system 提示词改为明确禁止声称图片已准备好，修复无图却发送成功口吻配文的问题。
- **取消退额度**：任务被取消（插件重载/关闭）时与普通失败一样退回本次扣除的绘图额度。
- **配文时间预算**：配文等待改为必须为发送阶段预留 15 秒以上（剩余不足 20 秒直接跳过），并修复配文拖尽预算后任务静默无通知的问题（超时且投递未完成时补发超时通知）。
- **SSRF 加固**：HTTP 参考图域名固定为解析出的 IP 直连并携带原 Host 头，校验与连接使用同一次解析结果，消除 DNS rebinding 绕过；HTTPS 保持证书校验兜底。

### 新增

- **参考图识图**（`reference_caption`，默认关闭）：用识图 Provider 为显式参考图生成视觉描述，拍照任务并入副脑输入、普通绘图追加到最终 prompt，失败自动跳过。
- **LLM 重试控制**（`llm_retry`，默认 1）：imago 发起的 LLM 调用（副脑/摘要/识图/配文）可重试错误的最大尝试次数，不再沿用 AstrBot 全局默认 5 次。

### 变更

- **生图链路日志**：补齐任务开始、节点尝试结束、参考图解析清单与终态汇总日志，失败/超时/投递失败以 ERROR 输出错误与尝试摘要。
- **日志体系清理**：删除无效的 `log_config` 一次性迁移代码；旧分组已废弃，历史配置需在 WebUI 手动设置顶层 `log_with_bot_id` 开关。

测试：130 项单元测试通过。

## 1.0.2 - 2026-08-16

- 修复参考图透传：引用消息改为事件阶段前台解析、无法获取时严格失败；来源统一支持本地路径、HTTP(S)、`data:` 与 `base64://`，按文件魔数识别、拒绝空响应，生成结果下载保持宽松。
- HTTP(S) 参考图下载改为手动限跳并逐跳 SSRF 校验（含重定向到内网拦截），初始与跳转校验失败统一为参考图异常。
- `/imago ref-upload` 与绘图共用参考图来源分类与 SSRF/大小限制，单次上传最多 20 张。
- `/画`、`/拍照`、`/imago draw/photo`、`/imago summary-set` 改用 `GreedyStr` 语义，含空格的描述不再被截断。
- 创建阶段仅透出白名单内的固定错误文案，防拼接泄漏。
- Gemini `fileData.fileUri` 自动补齐下载所需 API key。
- 新增“副脑降级时注入风格后缀”开关（默认关闭）：启用后副脑关闭或调用失败降级时，将风格预设、自定义提示词与默认第三方视角以低优先级后缀写入最终 prompt；副脑失败不再终止任务，降级使用原始描述。副脑正常完成时不注入后缀。
- 风格指导强化身份保真（写实/动漫/3D/电影感），Persona 默认自然第三方视角；`generate_persona_image` 新增 `camera` 参数。
- 日志配置精简：移除调试提级开关，`log_with_bot_id`（区分机器人实例）调整为顶级配置，日志等级统一在 WebUI 插件详情页调整。
- 新增 `llm_caption`（默认关闭）：出图成功或失败后由会话 Chat Provider 按当前人设生成一句简短配文随结果发送（配文在前、图片拼在其后）；`llm_caption_cm_context` 可选复用 ChatMemory 接管上下文（模式同 time_awareness），`llm_caption_pregen` 可在图片生成期间并行预生成成功版配文；失败或时间预算不足自动回退固定文案。

## 1.0.1 - 2026-07-29

- 插件及管理页展示名统一为 `IMAGO·映相`，并精简节点、副脑和识图 Provider 的配置说明。
- 副脑配置改用“启用副脑优化”和“副脑自定义提示词”；风格预设使用 `英文(中文)` 下拉项，并新增 `None(无)` 以跳过内置风格指导。
- 额度表支持按用户 ID、状态、额度、刷新日期和签到日期升降序排列。
- 多 API Key 改为每个节点独立按调用顺序轮换，不使用可能连续撞 Key 的随机选择。
- `/imago help` 改为按用途与当前权限分组的 T2I 帮助卡片，渲染失败时自动回退纯文本。

## 1.0.0 - 2026-07-25

- 普通绘图、Persona 出镜、消息/引用/URL 参考图与两个 LLM 绘图工具。
- OpenAI Image/Chat、Gemini、阿里云百炼、自定义端点，以及节点/模型 fallback、主节点选择和多 Key 轮换。
- Persona 外观摘要、参考图抽样、素材管理和 Plugin Page。
- 黑名单、无限额度白名单、每日额度重置、签到、管理员调额和额度页面。
- 异步任务状态、生成/图片投递/失败通知分离、取消落盘、任务缓存和模型级日志。
- 主动消息兼容装饰器前置分段；并发与上传限制动态生效。
- 百炼共用多模态协议、错误码透传和配置化参考图上限，不按模型名写死能力。
- 任务进度仅通过 `/imago status` 查询，不向主 LLM 注入后台任务快照。
- 普通 `failed` 自动退回本次实际扣费；Provider 响应无图或结果零可用统一使用 `no_output`，与超时及其他终态一样不退款。
- `/imago photo` 与 `/拍照` 的受理回复改用“当前人设”。
